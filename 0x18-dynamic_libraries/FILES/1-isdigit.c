#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit  -  checks if a string is digit.
 * @c: the char.
 *
 * Return: the digit converted.
 */

int _isdigit(int c)
{
	return (isdigit(c));
}
