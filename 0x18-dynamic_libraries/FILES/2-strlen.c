#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - string len converter.
 * @s: string.
 *
 * Return: the string.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
