#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcpy - string to be copied.
 * @dest: destination
 * @src: source.
 *
 * Return: the string copoied.
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
