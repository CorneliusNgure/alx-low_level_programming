#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isalpha - checks for the the alphabets.
 * @c: the char.
 *
 * Return: the char.
 */

int _isalpha(int c)
{
	return (isalpha(c));
}
